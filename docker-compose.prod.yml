version: "3.9"

services:
  frontend:
    build:
      args:
        - PUBLIC_URL=/
        - REACT_APP_AUTH_TOKEN=${AUTH_TOKEN}

  nginx:
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/ssl:/etc/nginx/ssl:ro
