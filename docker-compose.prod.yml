version: "3.9"

services:
  frontend:
    build:
      args:
        - PUBLIC_URL=/ThinkLingo

  nginx:
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/ssl:/etc/nginx/ssl:ro
